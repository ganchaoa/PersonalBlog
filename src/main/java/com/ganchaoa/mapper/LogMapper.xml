<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ganchaoa.dao.LogDao">
	<select id="getLogs" parameterType="int" resultType="Log">
		select * from t_log limit #{page-1}*#{limit},#{page}*#{limit}
	</select>
</mapper>